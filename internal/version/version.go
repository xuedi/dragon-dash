// Package version is the single source of truth for the release number.
//
// README.md's badge mirrors this constant; change both in the same commit so
// they cannot drift.
package version

// Version follows semantic versioning, pre-1.0.
const Version = "0.5.0"
