# Scrape config for the deployed stack.
# 60s is ample for smart plug power and room temperature; the storage
# arithmetic in ../../docs/fritzbox-metrics.md assumes it.

global:
  scrape_interval: 60s
  scrape_timeout: 30s
  external_labels:
    host: dragon

scrape_configs:
  # The board itself: CPU, memory, filesystems, hwmon temperatures.
  - job_name: node
    static_configs:
      - targets: ["node-exporter:9100"]

  # dragon-dash, which polls the FRITZ!Box and republishes it.
  - job_name: dragon-dash
    static_configs:
      - targets: ["dragon-dash:8080"]

  - job_name: prometheus
    static_configs:
      - targets: ["127.0.0.1:9090"]
